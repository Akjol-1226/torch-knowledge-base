# T-014 — modules/ingest/graph（langgraph 占位）

## 改了什么
- 新增 `backend/app/modules/ingest/graph.py`（IngestState TypedDict + 4 节点占位 graph）
- 新增 `backend/tests/unit/test_ingest_graph.py`（2 个单测）

## 为什么改
入库流水线的编排骨架。v0 节点全部占位（只 log + 写 trace），等 ADR-002 / PoC 完成后真实实现挂上来。

## 思路
- 4 节点：load_pdf → extract_pages → build_pageindex → persist
- 状态用 TypedDict（轻量、与 langgraph 0.6 API 兼容）
- `trace` 字段是测试用的「我经过这里了」记录，可以验证 graph 边连接正确

## 验收
- `uv run pytest tests/unit/test_ingest_graph.py -v` → 2 passed

## 关联
- plan T-014
- 设计文档「Ingest langgraph 状态图（占位）」

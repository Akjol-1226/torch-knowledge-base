# T-017 — 全量测试 + smoke test + curl 闭环

## 改了什么
- 没新增/修改代码，本 task 是验收 task
- 跑了：
  - 全量 pytest（24 passed）
  - ruff（All checks passed）
  - pyright（0 errors / 0 warnings）
  - uvicorn 真起 server + curl 4 个 endpoint

## 为什么改
T-006 ~ T-016 累计完成的骨架代码做一次端到端验证，确认「写过的 test 真跑通」+「真启 server 真发请求真有响应」。

## 思路
- 把 server 起在 8765 端口（避开默认 8000，防止冲突）
- 用 fake pdf 文件验证 /ingest/upload，再 GET /ingest/tasks 验证 task 写入
- smoke_test 没真调 LiteLLM（按约束：无真实 API key）；只验证模块 import 干净 + 脚本入口可启动

## 验收
- `uv run pytest -v` → 24 passed in 0.83s
- `uv run ruff check .` → All checks passed!
- `uv run pyright` → 0 errors, 0 warnings, 0 informations
- 启动后端：`curl localhost:8765/health` → `{"status":"ok"}`
- `curl /openapi.json` paths：`/chat/echo`, `/health`, `/ingest/tasks`, `/ingest/upload`（4 个全在）
- Upload + Tasks + Echo 三个业务 endpoint 真实联通

## 关联
- plan T-017

## 未做的事（与 plan 偏离，但符合任务约束）
- 没真调 LiteLLM（无真实 API key），smoke_test 只验证 import + 启动

# T-008 — app/main + /health

## 改了什么
- 新增 `backend/app/main.py`（FastAPI app + lifespan + /health）
- 新增 `backend/tests/integration/test_health.py`（2 个集成测试）

## 为什么改
后端 server 入口，验证 FastAPI + lifespan + OpenAPI schema 链路通。

## 思路
- `lifespan` async context manager 接 startup/shutdown 钩子，里面初始化日志
- `/health` 极简返回 `{"status":"ok"}`，给容器健康检查和监控用
- 测试用 ASGITransport + AsyncClient（FastAPI 推荐的 test client 模式，避免起真实 server 端口）

## 验收
- `uv run pytest tests/integration/test_health.py -v` → 2 passed
- `/openapi.json` 可访问，schema 含 `/health`

## 关联
- plan T-008
